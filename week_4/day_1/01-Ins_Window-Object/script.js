/* 
    Window:
     -The window is the object representation of your browser.
     -It is the global top-level object in which all of your JS code is running in. 
     -Each tab will have a global top level window object.
*/

// Logs window object using this
console.log("this: ");
console.log(this);

// Logs the document object
console.log("window.document: ");
console.log(window.document);

// Logs body of document
console.log("document.body: ");
console.log(document.body);
