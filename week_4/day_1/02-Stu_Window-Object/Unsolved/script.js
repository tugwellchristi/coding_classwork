// TODO: What will the following line of code log?
console.log(window);
// Logs the window object (browser), which is a space for documents

// TODO: What will the following line of code log?
console.log(window.document);
// Logs the document in the window object

// TODO: What will the following line of code log?
console.log(document.documentElement);
// Logs the root element of the object

// TODO: What will the following line of code log?
console.log(document.head);
// Logs the content in the <head> section

/* How would you log the `length` property of the `window` object?  
window.length */